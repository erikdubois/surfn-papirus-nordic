# CLAUDE.md — surfn-papirus-nordic

## Project overview

Papirus **nordic** folder colour, folders-only, inheriting `surfn-icons`. Generated by
`~/.bin/make-papirus-colour surfn nordic` from the installed Papirus theme.

## Current state

Ships `usr/share/icons/Surfn-Papirus-Nordic/` (places only). Packaged as `surfn-papirus-nordic-icons-git`, `depends=('surfn-icons-git')`.

## Patterns & decisions

- Folders only (generic folder*.svg recoloured to nordic); rest inherits surfn-icons.
  Symlinks dereferenced (0 broken). `icon-theme.cache` gitignored. Regenerate with make-papirus-colour.
